<%# model en lugar del scope de NEW para que sea EDIT %>
<%= form_with model: @restaurante, local: true, class: "form-plato" do |form| %>

  <% if @restaurante.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@restaurante.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @restaurante.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= form.label :titulo %><br>
    <%= form.text_field :titulo %>
  </p>
 
  <p>
    <%= form.label :direccion %><br>
    <%= form.text_area :direccion %>
  </p>
  
  <p class="btn btn-dark">
    <%= form.submit %>
  </p>

<% end %>